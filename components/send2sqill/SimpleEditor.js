import ReactPlayer from 'react-player/lazy'

export default function SimpleEditor({ videoPlayer, speed, foundMedia, setProgress, file, isFileUploaded, setFileUploaded, draft, clearTrims, removeTrim, trims, progress, onPlayerSelect, handleTrimPress, manualTrimStart, setManualTrimStart, duration, submitEnabled, submit }) {
  return (
    <div className="container">

    </div>
  )
}